{
  "name": "zod-to-json-schema",
  "version": "3.0.2",
  "description": "Converts Zod schemas to Json Schemas",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "dev": "jest --watch",
    "build": "rimraf dist && tsc && node copyPackageFiles.js",
    "dryrun": "jest && npm run build && cd dist && npm pub --dry-run"
  },
  "keywords": [
    "zod",
    "json",
    "schema",
    "conversion"
  ],
  "author": "Stefan Terdell",
  "contributors": [
    "Hammad Asif (https://github.com/mrhammadasif)"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/StefanTerdell/zod-to-json-schema"
  },
  "license": "ISC",
  "dependencies": {
    "zod": "^3.7.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/json-schema": "^7.0.7",
    "ajv": "^7.1.1",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.2",
    "typescript": "~4.2.4"
  },
  "types": "index.d.ts"
}
